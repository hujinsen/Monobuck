# Monologue Web 设计指南

> 版本: 2025-10-20
> 用途: 记录当前前端界面（侧栏 + Dashboard）设计语言、配色、字体、组件与后续扩展建议。

---
## 1. 品牌基调
高雅、前沿、克制的科技感。以深绿系（Starbucks 风格）+ 玻璃磨砂 + 渐变金色点缀，营造“专业 + 高价值”氛围。右侧内容区域为深色渐变背景，左侧为半透明磨砂玻璃层。

---
## 2. 配色体系 (CSS Variables)
```css
:root {
  --color-brand-deep: #036043;      /* 深主色 */
  --color-brand: #00754a;           /* 主品牌色 */
  --color-brand-accent: #00a271;    /* 强调/高亮 */
  --color-foam: #f5f8f6;            /* 浅背景 (浅色模式卡片) */
  --color-foam-tint: #eef4f1;       /* 分割线/浅边框 */
  --color-bean: #1e2c27;            /* 深背景 / 正文深色模式主色 */
  --color-bean-alt: #2a3b35;        /* 深背景层次 */
  --color-gold: #cba258;            /* 金色点缀 (标题/数字渐变尾部) */
  --color-amber: #ffc277;           /* 提示 / 轻强调 */
  --color-danger: #d64550;          /* 错误/危险动作 */
  --color-info: #2a8fb8;            /* 信息提示 */
  --color-gradient-start: #013224;  /* 页面主背景渐变起点 */
  --color-gradient-end: #0c4230;    /* 页面主背景渐变终点 */
}
```

### 2.1 使用规范
- 主交互按钮: 渐变 (brand-accent → brand-deep)
- 导航激活: 渐变 (brand → brand-deep)
- 重要数字、主标题: 白 → gold / brand → gold（浅色模式自动调整）
- 热力格: brand / brand-accent 透明度分层 (0.25/0.4/0.55/0.75)
- Banner 信息: brand / brand-accent 半透明叠加 + Blur
- 错误态: danger；确认/成功仍复用 brand / accent

### 2.2 语义层级建议
| 语义 | 用途 | 示例 |
| ---- | ---- | ---- |
| 主色 (brand) | 核心品牌标识 | 活动导航、主要按钮背景 |
| 深主色 (brand-deep) | 悬浮/Pressed 背景、渐变尾部 | 主要按钮 hover/active |
| 强调 (brand-accent) | 次级强调、进度条前段 | 进度条 / 次级按钮 |
| 金色 (gold) | 高价值视觉锚点 | 指标数字、徽章、标题渐变 |
| 琥珀 (amber) | 轻提示 / 温和注意 | Banner、轻状态提示 |
| 危险 (danger) | 破坏型操作 | 删除按钮、错误文本 |

---
## 3. 字体系统
```css
--font-sans-cn: 'SF Pro Display','SF Pro Text','PingFang SC','Helvetica Neue','Helvetica','Arial','Hiragino Sans GB','Microsoft YaHei','Source Han Sans SC','Noto Sans CJK SC',sans-serif;
--font-sans-en: 'SF Pro Display','SF Pro Text','Helvetica Neue','Helvetica','Arial',sans-serif;
--font-mono: 'SFMono-Regular','Menlo','Consolas',monospace;
```

| 层级 | 变量/值 | 用途 | 说明 |
| ---- | ------- | ---- | ---- |
| 页标题 | 28px (`--font-size-xl`) | 页面主标题 | 渐变文字 + 粗体 600 |
| 模块标题 | 16px (`--font-size-md`) | 面板标题 | 600，字距 0.5px |
| 正文 | 14px (`--font-size-base`) | 正常文本 | 行高 1.5 |
| 次级 | 12–13px (`--font-size-xs/sm`) | 标签/辅助 | 大写或微字距 |
| 指标数字 | 38px | 统计卡片主数值 | 英文字体栈优先 |

---
## 4. 间距与圆角
变量：`--sp-*` (2,4,6,8,10,12,16,20,24,32,40)
- 模块外间距：20–32
- 模块内边距：24（卡片），统计卡 20/24 组合
- 行内控件：按钮水平 16–18，垂直 12

圆角层次：
| Token | 数值 | 用途 |
| ----- | ---- | ---- |
| --radius-xs | 4px | 微型徽章/进度条 |
| --radius-sm | 6px | 导航条目 / 小按钮 |
| --radius-md | 10px | 输入框 / 常规组件 |
| --radius-lg | 18px | 卡片 / 统计面板 |
| --radius-round | 1000px | 胶囊按钮 / 进度条 |

---
## 5. 阴影与层次
- `--shadow-soft`: 主要卡片、导航、列表项浮起态
- `--shadow-inset`: 按钮按压态 / 内凹拟物

可选扩展：
```css
--shadow-lift: 0 10px 28px -8px rgba(0,0,0,0.55),0 2px 4px -1px rgba(0,0,0,0.4);
```

---
## 6. 组件说明
### 6.1 侧栏 (Glass Sidebar)
- 背景：`rgba(0,20,15,0.55)` + `backdrop-filter: blur(28px) saturate(180%)`
- 边框：`1px solid rgba(255,255,255,0.08)`
- 分组：品牌 / 导航 / 配额信息

### 6.2 导航项
状态 | 样式
---- | ----
默认 | 半透明渐变 0.09 → 0.15，柔和阴影
Hover | 亮度提升 (0.14→0.25)
Active | brand → brand-deep 渐变 + 强阴影 + 边框提亮
Focus | 外描边 gold

### 6.3 按钮 (拟物混合风)
- 外观：深色渐变 + 轻内高光 + 外投影
- Press：`transform: translateY(2px)` + inset 阴影
- 类型：`.btn.primary`, `.btn.ghost` （可扩展 `.btn.danger`, `.btn.outline`）

### 6.4 统计卡 (Stat Card)
- 背景半透明 + radial overlay 高光
- 数字渐变：暗色模式 (#fff → amber)，浅色模式 (brand → gold)

### 6.5 热力日历 (Calendar Heatmap)
- 35 cells 占位（5 周）
- 等级：无 / l1 / l2 / l3 / l4 采用透明度阶梯
- 可映射指标：当日转录词数 / 有效录音分钟数 / 活跃度指数

### 6.6 Banner 提示
- 渐变：brand 与 accent 半透明叠加
- 用途：激励语/里程碑提示

---
## 7. 交互与可访问性 (A11y)
- 所有导航为 `<button>`，支持键盘 Tab + Enter
- Calendar cell：`role=button` + 键盘激活逻辑
- Focus 统一：`:focus-visible { outline:2px solid var(--color-brand-accent) }`
- 渐变文本提供 `-webkit-background-clip` 与标准 `background-clip`
- 对比度：主文本 (#e8f2ed) 与深背景 (#013224~#0c4230) 对比 > 4.5:1

---
## 8. 响应式
断点 | 行为
---- | ----
≤ 920px | 侧栏缩至 200px，内容区内边距减小
≤ 720px | 侧栏抽屉化（`.sidebar.open`），可添加汉堡按钮控制

---
## 9. 数据接入建议 (后端 API 形状)
```jsonc
GET /api/stats -> { "totalWords": 47, "timeSavedSeconds": 22, "wpm": 70.5 }
GET /api/transcripts?limit=20 -> [ { "id":1, "createdAt":"2025-10-20T08:20:00Z", "source":"Edge", "text":"..." } ]
GET /api/sources -> [ { "name":"Monologue", "wordCount":40, "icon":"📝" } ]
GET /api/activity?month=2025-10 -> [ { "date":"2025-10-01", "words":123 } ]
```
前端映射：
- words → 热力等级（分位或固定阈值）
- sources 汇总排序 → Top sources

---
## 10. 后续建议 (Roadmap)
1. 主题切换：显式 Light / Dark Toggle
2. i18n：中英双语（`navigator.language` 自动初始）
3. 数据缓存：IndexedDB (transcripts, stats snapshot)
4. 动画：进入过渡使用 `opacity + translateY` 或 Web Animations API
5. 组件库提取：将按钮/卡片封装为 `components/` 模块
6. PWA：添加 Manifest + Service Worker 用于离线统计查看
7. 可视化：添加词频云 / 每小时使用分布图

---
## 11. 代码片段参考
### 11.1 视图切换
```js
function switchView(key){
  document.querySelectorAll('.view').forEach(v=>{
    v.classList.toggle('hidden', v.dataset.view!==key);
  });
}
```

### 11.2 日历生成（当前 Demo）
```js
for (let i=0;i<35;i++){ /* 伪随机等级 */ }
```

---
## 12. 命名约定
- 组件块：`.panel`, `.stat-card`, `.banner`
- 状态修饰：`.active`, `.hidden`, `.l1 ~ .l4`
- 动作按钮：`.btn.primary`, `.btn.ghost`

---
## 13. 版本记录
| 日期 | 版本 | 描述 |
| ---- | ---- | ---- |
| 2025-10-20 | v1 | 初始设计体系建立 |

---
如需扩展（新增主题 / 提取设计 Token / Storybook 文档化），可在此文件继续追加。
